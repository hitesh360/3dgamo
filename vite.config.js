import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages serves the repo at https://<user>.github.io/<repo>/
  // so the base must match the repository name.
  base: '/3dgamo/',
});
